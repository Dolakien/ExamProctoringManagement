# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - ExamProctoringManagement

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Build with dotnet
        working-directory: ./ExamProctoringManagement  # Chuyển vào thư mục chứa file .sln
        run: dotnet build ExamProctoringManagement.sln --configuration Release

      - name: Publish with dotnet
        working-directory: ./ExamProctoringManagement  # Chuyển vào thư mục chứa file .sln
        run: dotnet publish ExamProctoringManagement.sln -c Release -o ${{ github.workspace }}/publish  # Xuất ra thư mục gốc của workspace

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{ github.workspace }}/publish  # Đường dẫn đến thư mục đã xuất ra

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'ExamProctoringManagement'
          slot-name: 'Production'
          package: ${{ github.workspace }}/publish  # Đường dẫn đến thư mục đã xuất ra
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_D56AC3A64F5C4DBB873D191E64301C34 }}
